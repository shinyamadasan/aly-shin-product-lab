# Features

> Catalog by area, with status. The source of truth for whether a feature EXISTS —
> not whether it is good.

## Products

| Status | What it adds |
|---|---|
| Done | Add/Edit/Delete a product from the Product Admin page (`/admin`). Products were previously a hardcoded 6-item array (`src/lib/sample-data.ts`) with no way to add a new one at all -- they now load from and save to the `products` Supabase table (or `localStorage` in offline mode), the same dual-mode pattern as every other entity. A new product immediately appears in the Proof Day product picker and, once it has a proof batch, the Costing page's batch selector -- the same flow the original 6 products already went through. Delete is reference-gated: a product with any linked batches/costing/tasting/journal records can't be hard-deleted (set `status` to Paused instead); an unused product can be deleted outright. |

## Inventory

Delivered incrementally as 5 milestones; each one ships as its own working, tested slice.

| Milestone | Status | What it adds |
|---|---|---|
| M1 — Ingredient master, Inventory page, Need to Buy | Done | Create/edit ingredients (name, base unit, current quantity, low-stock threshold, target stock, optional expiration date, optional average unit cost). Stock-status pill (Out/Low/Good). Computed inventory value (qty × avg cost). Need to Buy list with suggested purchase quantity. |
| M2 — CSV import, ingredient aliases, ledger | Done | Upload a purchase receipt CSV (Import Purchase CSV page), auto-map or manually map columns, resolve rows to ingredients (saved alias → exact → normalized → manual — no fuzzy matching), preview with zero inventory impact, exclude/re-include rows, Confirm import increases inventory exactly once and records a weighted-average cost update, correct earliest-expiration-date, and one `inventory_transactions` entry per affected ingredient. Manually assigning a row saves an alias so the same receipt text auto-resolves next time. Inventory Timeline page shows every transaction, grouped by date, with ingredient/quantity/type/source. |
| M3 — Bake, deduct inventory, ledger | Done | Bake page: pick a Proof Day batch, resolve its formula (alias → exact → normalized → manual, shared with CSV import), enter "Batches made" (decimal, must be > 0) with a read-only computed pieces-produced readout, preview grouped deductions with current/needed/resulting quantity per ingredient. Confirm blocked while any row is unresolved or stock is insufficient; an explicit override checkbox allows an insufficient bake to proceed into negative stock. Confirm deducts each affected ingredient exactly once, records one `consume`/`bake` transaction per ingredient, and never touches average unit cost. Guarded against a fast double-click deducting twice. |
| M4 — Expiration, Dashboard cards | Done | Expiration-status badge (Expired / Expires today / Expires soon within 3 days / Good / none) on the Inventory page, rendered as its own badge next to — never merged with — the stock-status pill. Dashboard gains 3 summary cards: Low stock, Out of stock, Expiring, all driven by one `getInventorySummaryCounts()` call. |
| M5 — RPC atomicity | Done | Purchase and Bake confirmations use database-authoritative, atomic, concurrency-safe, mutation-receipted functions. |
| Claude Inventory Operator V1A | Implemented locally; production rollout blocked pending independent review and owner authorization | Claude Code structures a supplied physical count, while a deterministic owner-authenticated CLI performs safe matching/unit normalization, exact preview + approval binding, one atomic/idempotent batch count through the existing raw inventory authority, and authoritative read-back verification. No purchases, ingredient creation, OCR, browser automation, or AI API. |
| Product Lab MCP Slices 1–2 | Slice 1 merged; Slice 2 reviewer fixes implemented locally, pending targeted re-review | One local stdio server implements the same five owner-authenticated tools through one shared application service. Claude retains all five with Apply under `permissions.ask`. In a trusted checkout, Codex enables all five with `approvals_reviewer = "user"` and a per-tool Apply prompt; a real installed-client loopback rehearsal proved the prompt blocked execution until separate human approval. Apply remains narrow, owner-reauthenticated, atomic, and idempotent. No generic database access or other mutation category. |
| Operational friction cleanup V1 (Inventory) | Implemented locally, pending merge | Information-hierarchy pass, not an architecture change: the Inventory tab contract (`/inventory?tab=...`) and every old bookmark/redirect are unchanged, but the primary navigation now shows only Stock / Purchases / Manage Items -- Need to Buy folds into Stock as a "Low / Out" filter alongside a new "Expiring" filter and search; History moves to a secondary link. The daily Stock table drops Target, inventory Value, and per-row cost-certification state -- a healthy row renders no status tag at all, only Out/Low/expiration/reconciliation states do. Cost certification (`cost_reconciled_at`, `certify_ingredient_cost_baseline`, the `confirm_bake_v3` guard) is fully preserved server-side; the UI now reads it through one shared `isCostBaselineUncertified()` helper (replacing 3 independently-drifting inline copies) and surfaces it as a "Cost setup" summary plus plain-language per-item text in Manage Items instead of a red flag on nearly every Stock row. |
| Operational friction cleanup V1 (Purchases) | Implemented locally, pending merge | The manual purchase form no longer makes the operator decide whether a catalog Item exists first. Ingredient is a smart field: an exact normalized match (the app's own `normalizeIngredientName`) reuses that Item automatically; a near-match (word order, trailing s/es, or a small typo -- see `arePossibleNameMatch`) is surfaced with "Use X" or an explicit "Create anyway", never chosen or merged silently; an archived exact match blocks a duplicate and offers "Restore and use" via the existing restore behavior; several Items with the same normalized name block the purchase as an integrity problem. A genuinely new name becomes an Item only when the purchase is saved (no separate Create Item step), with its base unit inferred from the purchase unit (g/kg -> g, ml/L -> ml, pcs -> pcs) or chosen by the operator when the unit cannot be mapped safely. Purchases still post through `post_raw_purchase` with the same operation id; if the Item is created but the purchase fails, the operator is told so and a retry reuses that Item. Brand stays purchase context, never Item identity. CSV import is unchanged. |
| Operational friction cleanup V1 (Manage Items) | Implemented locally, pending merge | Manage Items is now an occasional-maintenance surface, not a permanent dashboard: it lands on a searchable master list with a "+ Add item" button -- the full Add/Edit form (all fields and validation unchanged) only opens on Add or Edit and returns to the list after save/cancel. Item rows are concise (name, category/unit, and exception tags only: "Cost needs verification", "Needs reconciliation"); one Manage control expands the numbers, latest purchase, purchase history, and every action (Edit, Adjust Stock, Certify/Re-certify cost, Archive). The per-row Buy shortcut is removed -- Purchases owns buying. "Review costs" narrows the list to Items needing verification. Archived Items sit in a collapsed section with Restore and the guarded Permanent delete. The stale "later milestones" help text is replaced with one current line. Certification semantics (`cost_reconciled_at`, `certify_ingredient_cost_baseline`, the `confirm_bake_v3` guard) are unchanged. |
| Operational friction cleanup V1 (final visual correction) | Implemented locally, pending merge | Display and navigation only. One shared human quantity formatter (`formatQuantity`, reusing `convertUnit`): 0.0047 kg shows as 4.7 g, 0.25 L as 250 ml, a non-zero quantity is never shown as zero -- used on Stock, Manage Items and Bake (stored values, base units and deductions unchanged). Purchases: the per-Item Log Purchase button is removed, By Item rows are compact (latest purchase, the stored total paid with the unit price shown separately, history behind one View control), a search covers item/brand/supplier, and the comparison explainer is a closed disclosure instead of a 420px sidebar. Manage Items: "+ Add item manually" (Purchases is the normal creation path), cost warnings only in cost-focused mode (Review costs, or `/inventory?tab=ingredients&focus=costs`), and the expanded Item separates Cost basis, Latest purchase (with total paid) and Stock value -- a value is only shown for a verified cost. Bake: descriptive batch options (product, version, pieces, date), a compact cost preflight linking to the focused cost view, deductions as a disclosure in the main flow instead of a right rail, and historical cost wording that says it was frozen at posting. No RPC, migration or authority change. |
| Operational friction cleanup V1 (Cost verification UX) | Implemented locally, pending merge | Operator language is now "Verify cost" (code and RPC keep `certify_*`). When an Item's latest purchase is usable (positive pack quantity and total, a unit that converts to the Item's base unit, finite positive unit cost), Manage shows the purchase, a suggested cost, and one "Verify this cost" button -- no evidence textbox: the evidence note the RPC requires is generated from that purchase (`Latest purchase: Calumet · Ayala · Sep 17, 2026 · PHP 19.00 / 50 g = PHP 0.38/g`; absent brand/supplier/date are omitted). No usable purchase -> "No usable purchase cost is available for this item yet." plus a secondary manual entry of real-world facts (total paid, quantity, unit) -- also reachable via "Enter a different cost manually" -- from which the app calculates the unit cost (converted to the item's base unit, e.g. `Manual cost basis: PHP 220.00 / 1 kg = PHP 0.22/g`) and generates the evidence note; nothing is typed as PHP/unit or as a note, and positive money is always required. Feedback is inline in the panel; in cost-focused mode each row shows the latest unit cost and a "Verify latest cost" action, and an Item just verified stays listed with its result until the mode is left. A gateway timeout is treated as an UNCERTAIN outcome: one RPC is sent, never retried, and a read-back of `cost_reconciled_at` / `average_unit_cost` decides verified / not saved / still uncertain. Unchanged: `certify_ingredient_cost_baseline` (evidence argument, optimistic concurrency, audit row), `confirm_bake_v3`'s guard, weighted-average costing, no bulk or auto verification. Logic lives in `src/lib/cost-verification.ts`. |
| Operational friction cleanup V1 (Bake recipe picker) | Implemented locally, pending merge | Bake's batch selector is now "Recipe to bake" and lists ONE current proof batch per product -- "current" is the first NON-VOIDED batch under Bake's existing per-product order (newest `dateMade`, loaded order breaks ties) -- a newer voided batch never displaces a valid one, voided batches stay reachable under older versions labeled "Voided", a product whose every batch is voided says so instead of showing a voided recipe as current, and `confirm_bake_v3` still refuses a voided batch; no new status, flag, column or migration. Every other batch stays reachable under a collapsed "Use an older version" (older batches grouped by product, same descriptive label), shown only when older batches exist. Choosing one makes it the real selection, shows "Using an older recipe version.", and never snaps back; a `?batch=<id>` deep link to an older batch is honored exactly and opens the disclosure, an invalid id falls back to the first product's current batch. Pure logic in `src/lib/bake-batch-option.ts` (`buildBakeBatchChoices`); everything downstream of `selectedBatchId` (formula, deductions, cost-verification guard, operation id, `confirmBake`) is unchanged. |
| Ops polish V2 | Implemented locally, pending merge | (1) Manual cost entry: editing paid / quantity / unit clears a definite failure message but never an uncertain one and never unlocks it; the unchanged-read-back timeout now says no saved change was found yet and to check again before retrying (no automatic retry). (2) Orders: after a successful Confirm, Complete, or Cancel of a confirmed/ready order, `OrdersPage` calls the parent's existing `loadSupabaseData` (`onStockChanged`) so the stock-readiness readout's `finishedStockMovements` is current; changes that move no stock (new -> cancelled, -> ready, edits) do not reload, a refused change reloads nothing extra, and Confirm stays decided by `confirm_order_with_reservation` (rule: `orderStatusChangeMovesStock`, tied by test to the transitions the repository routes through an RPC). (3) Bake: see the recipe-picker row -- current recipe is the newest non-voided batch. (4) Purchases: a NEW Item is created only after the purchase's quantity + unit convert to its base unit (`checkNewItemPurchaseUnit`, same `convertToBaseUnit` as the purchase effect), so a unit-only failure leaves no stray Item. (5) The Purchase form asks for a category (Ingredient default; Packaging, Consumable, Other) only when a genuinely new Item will be created, and `saveIngredient` writes it; existing Items are never re-categorized. Packaging bought this way appears in current stock, Purchases, Manage Items and the Selling Formats packaging picker (never Bake's formula picker); no automatic packaging consumption exists. (6) Operator copy says Item (purchase field label, purchase report column, empty stock state). No migration or schema change. |
| Cost verification + Inventory polish V3 | Implemented locally, pending merge | (1) Purchase facts first: the verification panel now leads with "Purchase to verify -- PHP 19.00 for 50 g" (brand · supplier, date beneath) and shows the derived "Calculated cost PHP 0.38/g" as a secondary line, with "Verify that this is what you paid and received. The app calculates the cost per g."; the collapsed cost-mode row reads "Latest purchase PHP 19 / 50 g" with a "Verify purchase" button. The latest-purchase and manual paths are one concept (same button, same "Calculated cost", "Enter purchase manually" / "Enter a different purchase manually"); still no PHP/unit input. (2) Button states: Verify purchase -> Saving... -> Checking result... (moves to Checking when the request is slow or fails at the transport level and the app is reading the Item back); never an open-ended "Verifying...". (3) Every request (pre-read, RPC, read-back) has a 15 s deadline (`CERTIFY_REQUEST_DEADLINE_MS`) that feeds the existing uncertain path, so the button always settles; still exactly one RPC, never retried. (4) A timeout with no saved change found is now an UNCERTAIN, locked outcome ("...Reload and check whether this item shows Verified before retrying.") rather than a re-submittable failure, since a slow request can still commit. (5) Close/reopen safety: a page-level attempt tracker refuses a second submit for an Item that is in flight or whose outcome is uncertain, even after its panel is closed and reopened; page messages name the Item. (6) After a verified result the full-page reload runs in the background instead of holding the button. Timeout diagnosis: the RPC body is O(1) (0.3 ms latest-row lookup and ~6-8 ms end to end at 1.2M ledger rows in a disposable Postgres; a row-lock wait surfaces as a coded 57014 at the 8 s statement timeout, not an uncoded gateway timeout), so no SQL change is made. No migration, RPC, schema or Bake-guard change. |
| Operational friction cleanup V1 (Bake) | Implemented locally, pending merge | Same information-hierarchy pass applied to Bake, no RPC/guard change. A Preflight block (Ingredients matched / Enough raw stock / Cost setup needed) replaces scanning the full ingredient list for problems: a fully-resolved recipe's mapping table collapses into a closed-by-default disclosure, an unresolved ingredient still shows its assignment picker directly (never hidden), and several uncertified ingredient costs collapse into one grouped Preflight line linking to Inventory's Manage Items instead of one red card per ingredient. The right-column deductions list becomes a closed-by-default "View ingredient deductions" disclosure (auto-open when something is short). Batch amount gets 0.5x/1x/2x quick-fill buttons that write into the same numeric field, not a separate mode. Stock correction (damage/giveaway/count-correction) moves behind an "Advanced" disclosure below Finished stock and Production history. Unchanged: `confirm_bake_v3`'s cost-certification and insufficient-stock guards, actual-pieces is still required and never defaulted from expected, and idempotency behavior. |

## Brand Foundation

| Status | What it adds |
|---|---|
| Done | `/brand` page: the single source of truth for Aly & Pon's current branding -- Business Name, Brand Status (Exploring/Provisional/Final -- the maturity of the branding decisions, not the business's operating stage), one-line description, target audience, Primary/Secondary/Background/Accent color (swatch + hex + copy, genuinely unset until chosen -- never a fabricated `#000000`), and one Brand Guidelines textarea covering aesthetic, photography style, keywords, mood, inspiration, and things to avoid. One page, one form, one always-active record -- no list, no create/delete flow. Deliberately excludes logo/profile-picture/cover-photo upload, Supabase Storage, typography, brand voice, CTA configuration, phrase lists, version history, multiple profiles, approvals, and AI generation -- each is unbuilt and would need its own separate authorization. |
| Done | Brand Presence section on the same `/brand` page: Website, Email, Preferred Handle, Facebook, Instagram, TikTok, YouTube -- the single source of truth for Aly & Pon's official online presence. Website renders as a plain clickable hyperlink; Email as a `mailto:` link; each social platform shows its saved handle (e.g. `@alyandpon`) as clickable text that opens its saved URL in a new tab -- no Open/Copy buttons anywhere. Handle and URL are stored separately per platform (never assumes a URL format). Adding a future platform (Threads, Pinterest, LinkedIn, a menu link) is a small config addition, not a page redesign. Deliberately excludes follower counts, analytics, posting, scheduling, and any social-management capability. |

## Marketing — Creative Pipeline

| Feature | Status | What it adds |
|---|---|---|
| PROP-034 — Daily Recommendation Readiness | Implemented, pending real-environment verification and Scheduled Task registration | A scheduled offline worker (`npm run creative-prep`) that advances the newest eligible Opportunity to a ready Creative Package before the owner opens the app, using a new deterministic, non-AI Creative Package initializer (`opportunity_brief` worker) instead of the placeholder `mock` worker or a paid AI API. Detects (never recovers) a suspiciously long-`running` Creative Job as a reporting-only failure signal. See `docs/ARCHITECTURE.md`'s "Daily Recommendation Readiness (PROP-034)" section and `planning/PROPOSALS.md`. |
| Wave D1 — Template Reel Authoring | Implemented, production activation off by default | The normal Content Generation path can now author a saved Reel as either filmed `capture_new` or deterministic `template_only`. Template Reel V1 is a silent, text-led, one-or-two-beat warm-open motion graphic: headline, beat text, CTA, brand mark, neutral accents, and 6-10s total duration. It omits camera framing, requires `spokenScript: null`, never uses `generate_visual`, and does not execute product-specific visuals, diagrams, or arbitrary scene-direction prose. Owner surfaces label them as no-filming Template Reels while Produce/Regenerate remain gated by trusted server-side video activation. Public CTAs follow the organic-engagement doctrine: engagement is earned by the idea, generic solicitations such as "tell us in the comments" are prohibited, and a CTA may simply be the closing creative beat. Owner-recorded limitation: Template Reel V1 is typography-only and renders no depiction of the subject; it is a truthful floor, not the intended Aly & Pon visual ceiling, and expressive visual treatments are a later production-capability wave. |

## Raw inventory authority

Selling Waves 0A–3 and Cost Baseline Repair are implemented. Verified counts, purchase/Bake
posting, production execution, finished-stock/order movements, exceptions, raw COGS, and separate
quantity/cost reconciliation facts are database-authoritative. See `docs/ARCHITECTURE.md` and the
`planning/SELLING_WAVE_*` records.

## Operations dashboard and navigation

| Feature | Status | Notes |
| --- | --- | --- |
| Dashboard as home (`/`) | Done | Business pulse, needs attention, finished stock and demand, inventory attention. Reuses `buildSellingSummary`, `deriveFinishedStockBalances` and the inventory-status helpers. No profit (see `planning/DASHBOARD_PROFIT_V1.md`). |
| Today at `/today` | Done | Unchanged apart from its address; `?job=` resume preserved, old `/?job=` links redirect. |
| Grouped navigation | Done | Operations / Marketing / More. Bake added to the sidebar; every route still reachable. |
