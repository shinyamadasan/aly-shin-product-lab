# Copy to .env.creative-workers.local and fill in real values.
# Only read by `node scripts/creative-workers/run.ts run-api` -- the export/import (manual)
# subcommands never read this file and never require an API key.

ANTHROPIC_API_KEY=
